const { test } = require('@playwright/test');
const { loadRuntimeData } = require('../../../helpers/iteration-matrix/dataLoader');
const { loginAsAdmin, logout } = require('../../../helpers/iteration-matrix/auth');
const { registerModuleSuite } = require('../../../helpers/allureHierarchy');
const { fileprocessingHelpers } = require('../../../helpers/iteration-matrix/fileprocessing.js');
const { fileprocessingSelectors } = require('../../../selectors/iteration-matrix/fileprocessing.selectors.js');

registerModuleSuite(test, __dirname);

async function closeSession(page) {
  await logout(page);
}

module.exports = {
  test,
  loadRuntimeData,
  loginAsAdmin,
  closeSession,
  fileprocessingHelpers,
  fileprocessingSelectors
};
