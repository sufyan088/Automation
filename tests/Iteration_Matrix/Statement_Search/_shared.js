const { test } = require('@playwright/test');
const { loadRuntimeData } = require('../../../helpers/iteration-matrix/dataLoader');
const { loginAsAdmin, logout } = require('../../../helpers/iteration-matrix/auth');
const { registerModuleSuite } = require('../../../helpers/allureHierarchy');
const { statementSearchHelpers } = require('../../../helpers/iteration-matrix/statementSearch.js');
const { statementSearchSelectors } = require('../../../selectors/iteration-matrix/statementSearch.selectors.js');

registerModuleSuite(test, __dirname);

async function closeSession(page) {
  await logout(page);
}

module.exports = {
  test,
  loadRuntimeData,
  loginAsAdmin,
  closeSession,
  statementSearchHelpers,
  statementSearchSelectors
};
